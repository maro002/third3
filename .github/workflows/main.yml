on: 
  push:
    branches: 
      - master2
  





jobs: 
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3 
      
      - name: "login"
        run: docker login --username ${{secrets.DOCKER_USERNAME}} --password ${{secrests.TOCKEN}}

      
